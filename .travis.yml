language: php

php:
  - 5.4
  - 5.5
  - 5.6

before_install:
    - composer self-update
    - composer install -n

install:
    - composer update --no-interaction --prefer-dist --prefer-lowest

script:
    - ./vendor/bin/phpspec run
