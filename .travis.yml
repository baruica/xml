language: php

php:
  - 5.4
  - 5.5
  - 5.6

before_install:
    - composer self-update

before_script:
    - php composer.phar install --dev --no-interaction --prefer-dist

script:
    - bin/phpspec run

matrix:
    allow_failures:
        - php: 5.6
    fast_finish: true
